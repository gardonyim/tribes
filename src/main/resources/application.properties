spring.datasource.url=jdbc:${DATABASE_URL}
spring.datasource.username=${USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.logging.level.org.hibernate.SQL=debug
spring.jpa.show-sql=true
logging.level.org.springframework.messaging=trace
logging.level.org.springframework.web.socket=trace
security.jwt-key=${JWT_KEY}
time.zone=${TIME_ZONE}

spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false

app.url=${ACTIVATION_URL}

#Email
spring.mail.username=${MAIL_USERNAME}
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS}
spring.mail.properties.mail.smtp.auth=${MAIL_AUTH}
spring.mail.templates.path=${MAIL_TEMPLATE_PATH}
email.resource.path=${MAIL_RESOURCE_PATH}
